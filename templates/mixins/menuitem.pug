mixin menuitem(name, isDefault)
  li.nav-item
    label(for=name)
      a.nav-link.btn.btn-sm(data-id=name)= name
    input(type='radio',style='display:none',checked=isDefault,name='selected',id=name,value=name)
    block